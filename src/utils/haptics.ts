export async function triggerTabSelectionHaptic(): Promise<void> {
  // Haptics are intentionally disabled for this polish pass.
  return Promise.resolve();
}

export async function triggerFavoriteToggleHaptic(): Promise<void> {
  // Haptics are intentionally disabled for this polish pass.
  return Promise.resolve();
}
